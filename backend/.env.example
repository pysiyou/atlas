# Database - PostgreSQL (REQUIRED)
# Replace with your actual database credentials
DATABASE_URL=postgresql://user:password@host:5432/database_name

# JWT Settings (REQUIRED)
# Generate a secure random key for production
SECRET_KEY=generate-a-secure-random-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (REQUIRED for production; optional in development)
# Comma-separated list of allowed origins. If unset and ENVIRONMENT=development, defaults to http://localhost:5173
CORS_ORIGINS=http://localhost:5173

# ENVIRONMENT=development enables CORS default http://localhost:5173 when CORS_ORIGINS is empty
# ENVIRONMENT=production (default) requires CORS_ORIGINS to be set
ENVIRONMENT=development

# Application
API_V1_PREFIX=/api/v1
PROJECT_NAME=Atlas Laboratory Management System

# File Storage
REPORTS_DIR=./storage/reports
UPLOADS_DIR=./storage/uploads
