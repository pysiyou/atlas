/**
 * Laboratory Constants (non-enum values only)
 *
 * For enum values (ContainerType, ContainerTopColor, SampleStatus, TestStatus),
 * import from '@/types' or '@/types/enums'
 */

// Currently no non-enum constants needed
// This file is kept for potential future lab-specific configuration values
