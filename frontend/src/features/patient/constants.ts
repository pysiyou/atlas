/**
 * Patient Constants (non-enum values only)
 *
 * For enum values (Gender, AffiliationDuration), import from '@/types' or '@/types/enums'
 */

export const AGE_RANGE_DEFAULTS: [number, number] = [0, 150];
export const AGE_RANGE_MIN = 0;
export const AGE_RANGE_MAX = 150;
