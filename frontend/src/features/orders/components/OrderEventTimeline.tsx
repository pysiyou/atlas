/**
 * Order activity timeline placeholder — implementation removed; rebuild here.
 */
import React from 'react';

interface OrderEventTimelineProps {
  orderId: number;
}

export const OrderEventTimeline: React.FC<OrderEventTimelineProps> = () => {
  return null;
};
