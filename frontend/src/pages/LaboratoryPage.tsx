/**
 * Laboratory page – re-export from feature (canonical implementation in features/lab/pages).
 * Kept so dynamic import('@/pages/LaboratoryPage') in App.tsx resolves.
 */
export { Laboratory } from '@/features/lab/pages';
